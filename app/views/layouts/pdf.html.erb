<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= content_for?(:title) ? yield(:title) : "Document" %></title>
  <%= wicked_pdf_stylesheet_link_tag 'pdf' %>

<style>
  @font-face {
    font-family: 'Noto Serif Ethiopic';
    src: url("data:font/truetype;base64,<%= WickedPdf::Helper.asset_base64('NotoSerifEthiopic-VariableFont_wdth,wght.ttf') %>") format('truetype');
    font-weight: 300;
    font-style: normal;
  }

  body.pdf {
    font-family: 'Noto Serif Ethiopic', serif;
    font-size: 20px; /* Adjust globally */
    font-weight: 300;
    line-height: 1.6;
    color: #000;
  }

</style>

</head>
<body class="pdf">
  <%= yield %>
</body>
</html>
